<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:RecipeApp.Mobile.ViewModels"
             xmlns:models="clr-namespace:RecipeApp.Mobile.Models"
             xmlns:converters="clr-namespace:RecipeApp.Mobile.Converters"
             x:Class="RecipeApp.Mobile.MainPage"
             Title="Recipe Book">

    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:ImageFileNameToSourceConverter x:Key="ImageConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.BindingContext>
        <viewModels:MainViewModel />
    </ContentPage.BindingContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header with Language Switcher -->
        <Grid Grid.Row="0" BackgroundColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}" Padding="15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" 
                   Text="Recipe Book" 
                   FontSize="24" 
                   FontAttributes="Bold" 
                   TextColor="White" 
                   VerticalOptions="Center" />

            <Button Grid.Column="1" 
                    Text="🔍" 
                    BackgroundColor="Transparent" 
                    TextColor="White" 
                    FontSize="20"
                    Clicked="OnSearchClicked" />

            <Button Grid.Column="2" 
                    Text="🌐" 
                    BackgroundColor="Transparent" 
                    TextColor="White" 
                    FontSize="20"
                    Clicked="OnLanguageClicked" />
        </Grid>

        <!-- Search Bar (Initially Hidden) -->
        <SearchBar x:Name="SearchBar" 
                   Grid.Row="1" 
                   Placeholder="Search recipes..." 
                   Text="{Binding SearchText}" 
                   IsVisible="False"
                   Margin="15,5" />

        <!-- Recipe List -->
        <RefreshView Grid.Row="2" IsRefreshing="{Binding IsBusy}">
            <CollectionView ItemsSource="{Binding FilteredRecipes}"
                           SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:Recipe">
                        <Grid Margin="15,10">
                            <Frame BackgroundColor="{AppThemeBinding Light=White, Dark={StaticResource Gray900}}"
                                   CornerRadius="10"
                                   HasShadow="True"
                                   Padding="15">
                                <Frame.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:MainViewModel}}, Path=GoToRecipeDetailCommand}"
                                                         CommandParameter="{Binding .}" />
                                </Frame.GestureRecognizers>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <!-- Recipe Image -->
                                    <Image Grid.Column="0"
                                           Source="{Binding ImageFileName, Converter={StaticResource ImageConverter}}"
                                           Aspect="AspectFill"
                                           HeightRequest="80"
                                           WidthRequest="80" />

                                    <!-- Recipe Details -->
                                    <StackLayout Grid.Column="1" Margin="15,0,0,0">
                                        <Label Text="{Binding Name.English}" 
                                               FontSize="18" 
                                               FontAttributes="Bold" 
                                               LineBreakMode="TailTruncation" />
                                        
                                        <Label Text="{Binding Category.Name.English}" 
                                               FontSize="14" 
                                               TextColor="{StaticResource Primary}" />
                                        
                                        <Label Text="{Binding Description.English}" 
                                               FontSize="12" 
                                               TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray300}}"
                                               LineBreakMode="TailTruncation"
                                               MaxLines="2" />
                                        
                                        <Label FontSize="11" 
                                               TextColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource Gray400}}">
                                            <Label.FormattedText>
                                                <FormattedString>
                                                    <Span Text="Prep: " />
                                                    <Span Text="{Binding PrepTime}" />
                                                    <Span Text=" | Cook: " />
                                                    <Span Text="{Binding CookTime}" />
                                                </FormattedString>
                                            </Label.FormattedText>
                                        </Label>
                                    </StackLayout>
                                </Grid>
                            </Frame>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>

                <CollectionView.EmptyView>
                    <StackLayout VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand">
                        <Label Text="No recipes found" 
                               FontSize="18" 
                               TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray300}}"
                               HorizontalOptions="Center" />
                        <Label Text="Try adjusting your search or filters" 
                               FontSize="14" 
                               TextColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource Gray400}}"
                               HorizontalOptions="Center" />
                    </StackLayout>
                </CollectionView.EmptyView>
            </CollectionView>
        </RefreshView>

        <!-- Banner Ad Placeholder -->
        <Frame Grid.Row="3" 
               BackgroundColor="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray800}}" 
               HeightRequest="50" 
               Margin="0"
               CornerRadius="0"
               HasShadow="False">
            <Label Text="Banner Ad Placeholder" 
                   FontSize="12" 
                   TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray300}}"
                   HorizontalOptions="Center" 
                   VerticalOptions="Center" />
        </Frame>
    </Grid>
</ContentPage>
